#include "Header.h"

void readClassCSV(ifstream& classList, Student* pHead) // Assume storing student as doubly linked list
{
	while (!classList.eof())
	{
		if (pHead == nullptr)
		{
			
		}
		getline(classList, )
	}
}